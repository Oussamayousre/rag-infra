apiVersion: v1
kind: Service
metadata:
  name: rag-api-service
  namespace: app
spec:
  selector:
    app: rag-api               # ← matches pods with this label
  ports:
    - port: 8501               # service port
      targetPort: 8501         # container port
  type: ClusterIP              # ← internal only